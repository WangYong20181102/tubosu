<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心 -->
    <solid android:color="@color/color_icon" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/color_icon" />
    <!-- 圆角 -->
    <corners android:radius="8dp" />
    <!-- 边距 -->
    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp" />

</shape>